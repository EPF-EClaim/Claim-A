
<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.f"
    xmlns:m="sap.m"
    xmlns:unified="sap.ui.unified"
    xmlns:table="sap.ui.table">

    <!-- <VBox width="100%" rowGap="1rem">
        <OverflowToolbar height="3rem" style="Clear">
            <Title id="reqFormDetailTitle" text="{i18n>req_form_detail_title}" level="H3"/>
            <ToolbarSpacer/>
            <Button text="Create Item" type="Emphasized" icon="sap-icon://add" press=".onPressAddItem"/>
            <Button text="Edit"/>
            <Button text="Duplicate"/>
            <Button text="Delete" type="Reject" press=".onDeleteSelected"/>
        </OverflowToolbar>

        <VBox rowGap="1rem">
            <table:Table
                id="req_item_table"
                rows="{/rows}"
                columnHeaderHeight="3rem"
                selectionMode="MultiToggle"
                ariaLabelledBy="reqFormDetailTitle"
                width="auto"
                rowActionCount="3">

                <table:Column width="25rem">
                    <Label text="Claim Type"/>
                    <table:template>
                        <Text text="{claim_type}" wrapping="false"/>
                    </table:template>
                </table:Column>

                <table:Column width="15rem">
                    <Label text="Estimated Amount"/>
                    <table:template>
                        <unified:Currency value="{est_amount}" currency="{currency_code}"/>
                    </table:template>
                </table:Column>

                <table:Column width="15rem">
                    <Label text="Estimated No. of Participant"/>
                    <table:template>
                        <Text text="{est_no_of_participant}" wrapping="false"/>
                    </table:template>
                </table:Column>

                <table:rowActionTemplate>
                    <table:RowAction>
                        <table:items>
                            <table:RowActionItem icon="sap-icon://edit" text="Edit"/>
                            <table:RowActionItem icon="sap-icon://duplicate" text="Duplicate"/>
                            <table:RowActionItem type="Delete" press=".onRowDelete"/>
                        </table:items>
                    </table:RowAction>
                </table:rowActionTemplate>

            </table:Table>
        </VBox>
    </VBox> -->

</core:FragmentDefinition>
