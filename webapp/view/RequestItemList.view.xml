
<mvc:View xmlns:unified="sap.ui.unified" xmlns:columnmenu="sap.m.table.columnmenu" xmlns:table="sap.ui.table" xmlns:m="sap.m"
    controllerName="claima.controller.RequestItemList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:tnt="sap.tnt"
    xmlns:cards="sap.f.cards"
    xmlns:f="sap.f"
    xmlns:l="sap.ui.layout"
    xmlns:cssgrid="sap.ui.layout.cssgrid"
    xmlns:w="sap.ui.integration.widgets"
    xmlns:card="sap.f.cards"
    xmlns:core="sap.ui.core"
    xmlns:Icon="sap.ui.core.Icon"
    xmlns:ShellBar="sap.f.ShellBar"
    xmlns:layout="sap.ui.layout"
    displayBlock="true"
    height="100%"
    width="100%">

    <mvc:content>
        <m:VBox width="100%">
            <m:OverflowToolbar style="Clear">
                <m:ToolbarSpacer />
                <m:Button text="Create Item" type="Emphasized" icon="sap-icon://add" press="onPressAddItem"></m:Button>
                <m:Button text="Edit" />
                <m:Button text="Duplicate" />
                <m:Button text="Delete" type="Reject"/>
            </m:OverflowToolbar>
            <m:VBox rowGap="1rem">
                <m:Title text="My Request (#)" titleStyle="H3" />
                <table:Table 
                    id="req_item_table"
                    rows="{/rows}"
                    columnHeaderHeight="3rem"
                    selectionMode="MultiToggle"
                    ariaLabelledBy="title"
                    width="auto">
                    <table:Column  width="25rem">
                        <m:Label text="Claim Type"></m:Label>
                        <table:template>
                            <m:Text text="{claim_type}" wrapping="false" />
                        </table:template>
                    </table:Column>
                    <table:Column  width="20rem">
                        <m:Label text="Estimated Amount"></m:Label>
                        <table:template>
                            <unified:Currency value="{est_amount}" currency="{curenncy_code}"/>
                        </table:template>
                    </table:Column>
                    <table:Column  width="20rem">
                        <m:Label text="Estimated No. of Participant"></m:Label>
                        <table:template>
                            <m:Text text="{est_no_of_participant}" wrapping="false" />
                        </table:template>
                    </table:Column>
                    <table:rowActionTemplate>
                        <table:RowAction >
                            <table:RowActionItem icon="sap-icon://attachment" text="Attachment" press="fnPress()"></table:RowActionItem>
                        </table:RowAction>
                    </table:rowActionTemplate>
                </table:Table>
            </m:VBox>
        </m:VBox>
    </mvc:content>
    
  	
</mvc:View>